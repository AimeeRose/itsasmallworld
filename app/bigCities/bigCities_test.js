'use strict';

describe('myApp.bigCities module', function() {

  beforeEach(module('myApp.bigCities'));

  describe('bigCities controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var bigCitiesCtrl = $controller('View2Ctrl');
      expect(bigCitiesCtrl).toBeDefined();
    }));

  });
});